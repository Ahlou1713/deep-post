import Title from "antd/es/typography/Title";
import React from "react";

interface Props {
    iconName: String,
}

const Tool = ({iconName}: Props) => {
  return <>
  {}
  <Title></Title>
  </>;
};

export default Tool;
